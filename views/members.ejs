<%- include('templates/header') %>

    <div class="container-fluid d-flex flex-column align-items-center gap-3 mt-5">
        <h3 class="text-center mt-3">Hello, <%= userName %>,</h3> <br>

        <div class="row ">
            <% for(let i=0; i<imageFiles.length; i++) { %>
                <img class="col-sm" src="/images/<%= imageFiles[i] %>" alt='this is a pokemon' />
                <% } %>
        </div>


        <a href="/logout"><button class="btn btn-primary mb-5"> Logout </button></a>
    </div>

    <%- include('templates/footer') %>